cat config/resolv.conf >> /etc/resolv.conf

cat config/hosts >> /etc/hosts

cat config/named.conf.local > /etc/bind/named.conf.local

cat config/db.flpp > /etc/bind/db.flpp
cat config/db.pplf > /etc/bind/db.pplf